
// IN CSS AND HTML THERE WAS A COMENT NOW IN JAVASCRIP OR JS THERE IS DOCUMENT IT IS ALSO COMENT BUT IT CALLED AS DOCUMENT TO WRITE IT CTRL + ?/

// hear i have made let arr arrar

let arr = [1,2,3,4,5,1]

// hear i have made let arr2 arrar

let arr2=[8,9,19]

// hear i have made let arr3 arrar

let arr3=[20,21,22]

// array will be always in square bracket []

// iam trying to find arr value in position [0] it shows 1

console.log(arr[0])

// iam trying to find arr value in position [1] it shows 2

console.log(arr[1])

// iam trying to find arr value in position [2] it shows 3

console.log(arr[2])

// to find lenth console.log(arr.length)

console.log(arr.length)

// Index of if the element is not present -1 is shown in console

// hear i have call arr and give it indexof(1) console.log(arr.indexOf(1))

console.log(arr.indexOf(1))

// Last Index of if the element is not present -1 is shown in console

console.log(arr.lastIndexOf(1))

// it will do riverce console.log(arr.reverse())

// it changes the orignal arry so use with caution

// console.log(arr.reverse())

// to join array by console.log(arr.join("aa"))

console.log(arr.join("aa"))

// slice to no midile by console.log(arr.slice(0,2),arr)

// slice does not modiify the original array

console.log(arr.slice(0,2),arr)

// it should be used with caution as it modifies the original array

// console.log(arr.splice(0,2),arr)

// arr.push(6)

// console.log(arr)

// arr.pop()

// console.log(arr)

// arr.shift()

// console.log(arr)

// arr.unshift(9)

// console.log(arr)

// changing element of array

// arr[3]=20

// console.log(arr)

console.log(arr.concat(arr2,arr3))

// tto convert into string

console.log(arr.toString())


// reverse this string


const str="Saksham Jaiswal Hero"

// lassiaJ mahskaS

// first i have a string named str i have writern inside string Saksham Jaiswal and i have got tsk to reverse it so i have write console.log() inside thi bracket i have split this str then i have reverse it so i have write .reverse then it became reverse so we have to join the array so i have add .join("") so the result came as "lassiaJ mahskaS"the code const str="Saksham Jaiswal",,console.log(str.split("").reverse().join(""))



const wrongName = "Saksham/Jaiswal/and/wrong"

// Saksham Jaiswal and wrong

// i have made a string named wrongName its value is "Saksham/Jaiswal/and/wrong" so heare i have got a task to take out / and space there so i have writern console.log() inside () bra

console.log(wrongName.split("/").join(" "))

// first i have made a string and i have converted into array by split and gave 0 and 1 and conveted into string using split and reverse it by using reverse and join it by using join code is const str="Saksham Jaiswal" const str0=str.split(" ")[0].split("").reverse().join("") const str1=str.split(" ")[1].split("").reverse().join("") console.log( `${str0} ${str1}`)

const str0 = str.split(" ")[0].split("").reverse().join("")
const str1 = str.split(" ")[1].split("").reverse().join("")

// oreH mahskaS lawsiaJ

console.log(str.split("").reverse().join(""))

// Docmunt earch and every line of your code